server:
  port: 8761

spring:
  application:
    name: Eureka Server
  output:
    ansi:
      enabled: always

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
  server:
    enable-self-preservation: false